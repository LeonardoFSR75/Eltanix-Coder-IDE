# SecureBERT 2.0 local wrapper

This directory contains a local Python package wrapper for the Cisco SecureBERT 2.0 research repository.

It is installed in editable mode from the project workspace to allow the backend to import the upstream research code without requiring a package index publication.
